import React from "react";

const Logo: React.FC = () => (
  <div className="fixed left-4 bottom-4 z-50">
    <img src="/logo.png" alt="Logo" style={{ width: 180, height: "auto" }} />
  </div>
);

export default Logo;
